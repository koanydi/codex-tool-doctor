# 参与开发

本项目用于解决第三方Responses API与Codex工具调用之间的兼容问题。提交修复时，请说明触发条件、实际行为、预期行为以及验证方式。

## 本地开发

使用Node.js22或更新版本：

```sh
npm ci --ignore-scripts
npm test
npm run gui
```

`src/engine.mjs`提供CLI和GUI共用的操作逻辑；`src/probe.mjs`负责协议对照；`src/verify.mjs`负责真实客户端验证；`src/patch.mjs`处理安装、回滚与恢复。启动器位于项目根目录和`scripts/bootstrap-*`。

测试使用隔离配置和回环模拟服务。不要将个人Codex配置或真实API密钥作为测试夹具。真实客户端的可选验收、环境安装检查见[验证指南](docs/验证指南.md)。

## 变更要求

- 新的兼容规则应提供失败和成功对照，不能只凭单次错误推荐修复。
- 安装必须在候选验证通过后执行，保留备份、回滚与并发保护。
- 平台相关变更需保留Windows、macOS和Linux入口的参数边界与错误返回。
- 文档应描述当前行为及限制，避免加入聊天记录、机器专属路径和未验证的兼容承诺。

## 构建发布包

```sh
npm run package
```

产物位于`dist/`：带版本号的ZIP和`SHA256SUMS.txt`。包内包含平台启动器、程序、用户文档和smol-toml依赖；不包含Git元数据、测试私钥、模拟服务器、开发日志或本机配置。相同文件内容生成相同ZIP。

发布前完成测试与依赖审计。三系统CI验证程序、启动器与环境准备；发布包生成后应解压运行一次。真实Codex终端验证受本机策略影响，必须如实记录结果。
